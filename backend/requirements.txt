fastapi==0.115.6
uvicorn[standard]==0.34.0
python-dotenv==1.0.1
sqlalchemy==2.0.36
pydantic==2.10.6
pydantic-settings==2.7.1
email-validator==2.2.0
passlib==1.7.4
bcrypt==3.2.2
python-jose[cryptography]==3.3.0
python-multipart==0.0.20
pytest==8.3.5
gunicorn==23.0.0
alembic==1.17.1
jinja2==3.1.4
# Two-Factor Authentication
pyotp==2.9.0
qrcode[pil]==8.0

# Version 2.0 Advanced Features
twilio==8.11.1  # SMS MFA
web3==6.15.1    # Blockchain integration
httpx==0.28.1   # Async HTTP client (already in testing, moved here)

# Rate Limiting
slowapi==0.1.9
# Stripe Payment Integration
stripe==14.4.1
# WebSocket Real-time Features
python-socketio==5.14.3

# File type detection
python-magic==0.4.27

# Cloud storage (S3, DigitalOcean Spaces, Cloudflare R2)
boto3==1.42.4

# Testing
pytest-asyncio==0.25.1
pytest-cov==6.0.0

# Turso (libSQL) - Managed SQLite database service
# REQUIRED: sqlalchemy-libsql for SQLAlchemy integration
# Note: Requires Rust toolchain to compile on Windows
# Install Rust from: https://rustup.rs/
# Or use Docker for deployment (includes pre-built packages)
sqlalchemy-libsql==0.2.0
requests==2.31.0

# MongoDB - Optional (for blog/advanced features only)
# Motor 3.7.1 requires pymongo>=4.9,<5
# Use latest motor for Python 3.13 compatibility
# Uncomment if you need MongoDB features:
# pymongo>=4.9,<5.0
# motor>=3.7.0
