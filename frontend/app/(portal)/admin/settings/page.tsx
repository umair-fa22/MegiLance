// @AI-HINT: Portal route for Admin Settings under the (portal) layout.
'use client';

import AdminSettings from './AdminSettings';

const AdminSettingsPage = () => {
  return <AdminSettings />;
};

export default AdminSettingsPage;
