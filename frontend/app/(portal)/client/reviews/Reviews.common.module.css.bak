/* AI-HINT: Common styles for the modernized Client Reviews page. Defines the main layout, header, controls, card grid, and editor form. */

.formGroup {
  margin-bottom: 1rem;
}

.formLabel {
  display: block;
  margin-bottom: 0.5rem;
}

.helperText {
  margin-bottom: 1rem;
  color: var(--text-secondary);
}

.page {
  padding: var(--spacing-6) var(--spacing-8);
}

.container {
  max-width: 1200px;
  margin: 0 auto;
  display: grid;
  gap: var(--spacing-6);
}

.header {
  margin-bottom: var(--spacing-2);
}

.title {
  font-family: var(--font-family-headings);
  font-size: var(--font-size-xxl);
  font-weight: var(--font-weight-bold);
}

.subtitle {
  font-size: var(--font-size-md);
  margin-top: var(--spacing-1);
}

.controlsSection {
  padding: var(--spacing-4);
  border-radius: var(--border-radius-lg);
}

.filters {
  display: flex;
  gap: var(--spacing-4);
  flex-wrap: wrap;
}

.searchInput {
  flex-grow: 1;
  min-width: 250px;
}

.grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
  gap: var(--spacing-5);
}

.card {
  border: 1px solid transparent;
  border-radius: var(--border-radius-lg);
  padding: var(--spacing-5);
  display: flex;
  flex-direction: column;
  gap: var(--spacing-4);
  transition: transform 200ms ease, box-shadow 200ms ease;
}

.card:hover {
  transform: translateY(-4px);
}

.cardHeader {
  display: flex;
  align-items: center;
  gap: var(--spacing-4);
}

.headerText {
  display: flex;
  flex-direction: column;
}

.freelancerName {
  font-weight: var(--font-weight-semibold);
  font-size: var(--font-size-md);
}

.projectName {
  font-size: var(--font-size-sm);
}

.cardBody {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-3);
}

.reviewText {
  font-size: var(--font-size-sm);
  line-height: 1.6;
}

.cardFooter {
  margin-top: auto;
  padding-top: var(--spacing-3);
  font-size: var(--font-size-xs);
  border-top: 1px solid transparent;
}

.emptyState {
  grid-column: 1 / -1;
  text-align: center;
  padding: var(--spacing-10) var(--spacing-5);
  border-radius: var(--border-radius-lg);
}

.emptyState h3 {
  font-family: var(--font-family-headings);
  font-size: var(--font-size-lg);
  font-weight: var(--font-weight-semibold);
}

.emptyState p {
  margin-top: var(--spacing-2);
  font-size: var(--font-size-md);
}

.paginationBar {
  display: flex;
  justify-content: center;
  align-items: center;
  gap: var(--spacing-4);
  margin-top: var(--spacing-4);
}

.paginationInfo {
  font-size: var(--font-size-sm);
  font-weight: var(--font-weight-medium);
}

.editor {
  padding: var(--spacing-6);
  border-radius: var(--border-radius-xl);
}

.sectionTitle {
  font-family: var(--font-family-headings);
  font-size: var(--font-size-xl);
  font-weight: var(--font-weight-bold);
  margin-bottom: var(--spacing-5);
}

.editorForm {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-5);
}

.editorActions {
  display: flex;
  justify-content: flex-end;
  gap: var(--spacing-3);
  margin-top: var(--spacing-2);
}

.ratingLabel {
  font-weight: var(--font-weight-medium);
  font-size: var(--font-size-sm);
}

.ratingSelector {
  display: flex;
  align-items: center;
  gap: var(--spacing-2);
}

.ratingStarButton {
  background: none;
  border: none;
  font-size: 1.5rem;
  cursor: pointer;
  padding: 2px;
  transition: transform 0.15s ease;
}

.ratingStarButton:hover {
  transform: scale(1.2);
}

.ratingStarActive {
  color: #f59e0b;
}

.ratingStarInactive {
  color: #d1d5db;
}

/* Toolbar */
.toolbar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: var(--spacing-4);
  flex-wrap: wrap;
  margin-bottom: var(--spacing-4);
}

.toolbarLeft {
  display: flex;
  gap: var(--spacing-3);
  align-items: center;
  flex: 1;
  min-width: 0;
}

.toolbarRight {
  display: flex;
  gap: var(--spacing-3);
  align-items: center;
}

.resultsSummary {
  font-size: var(--font-size-sm);
  opacity: 0.65;
  margin-bottom: var(--spacing-3);
}

.submitError {
  color: var(--color-error, #e81123);
  background: rgba(232, 17, 35, 0.08);
  border: 1px solid rgba(232, 17, 35, 0.2);
  border-radius: var(--border-radius-md);
  padding: var(--spacing-3) var(--spacing-4);
  font-size: var(--font-size-sm);
  margin-bottom: var(--spacing-4);
}

.skeleton.card {
  gap: var(--spacing-4);
}

.ratingSkeleton {
  margin-top: var(--spacing-2);
  margin-bottom: var(--spacing-2);
}

.srOnly {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border: 0;
}

.isNotVisible { opacity: 0; transform: translateY(20px); }
.isVisible { opacity: 1; transform: translateY(0); transition: opacity .5s ease-out, transform .5s ease-out; }

@media (max-width: 768px) {
  .page {
    padding: var(--spacing-4);
  }
  .toolbar {
    flex-direction: column;
    align-items: stretch;
  }
  .toolbarLeft,
  .toolbarRight {
    flex-direction: column;
  }
}

