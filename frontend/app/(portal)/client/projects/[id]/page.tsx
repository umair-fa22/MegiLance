// @AI-HINT: Portal route for Client Project Detail under the (portal) layout.
'use client';

import ProjectDetail from './ProjectDetail';

const ProjectDetailPage = () => {
  return <ProjectDetail />;
};

export default ProjectDetailPage;
