/* AI-HINT: Common styles for Client Freelancers page: filters, grid, cards, and empty state. */

/* AI-HINT: Common styles for the main Freelancers page. Defines layout, grid, and controls. */

.page {
  padding: var(--spacing-6) 0 var(--spacing-10);
}

.container {
  max-width: var(--max-width-wide);
  margin: 0 auto;
  padding: 0 var(--spacing-5);
  display: flex;
  flex-direction: column;
  gap: var(--spacing-6);
}

.header {
  opacity: 0;
  transform: translateY(20px);
  transition: opacity 500ms ease, transform 500ms ease;
  text-align: center;
}

.header.isVisible {
  opacity: 1;
  transform: translateY(0);
}

.title {
  font-family: var(--font-family-headings);
  font-size: var(--font-size-xxxl);
  font-weight: var(--font-weight-bold);
  margin-bottom: var(--spacing-2);
}

.subtitle {
  font-size: var(--font-size-lg);
  max-width: 60ch;
  margin: 0 auto;
}

.controlsSection {
  padding: var(--spacing-4);
  border-radius: var(--border-radius-lg);
  position: sticky;
  top: var(--spacing-4);
  z-index: 10;
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
}

.filters {
  display: flex;
  flex-wrap: wrap;
  gap: var(--spacing-3);
  align-items: center;
}

.searchInput {
  flex-grow: 1;
  min-width: 250px;
}

.grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(340px, 1fr));
  gap: var(--spacing-5);
  opacity: 0;
  transform: translateY(20px);
  transition: opacity 500ms ease 200ms, transform 500ms ease 200ms;
}

.grid.isVisible {
  opacity: 1;
  transform: translateY(0);
}

.skeletonCard {
  border-radius: var(--border-radius-lg);
  padding: var(--spacing-5);
  display: flex;
  flex-direction: column;
  gap: var(--spacing-4);
}

.skeletonHeader {
  display: flex;
  align-items: center;
  gap: var(--spacing-4);
}

.skeletonHeaderText {
  flex-grow: 1;
  display: flex;
  flex-direction: column;
  gap: var(--spacing-2);
}

.error,
.emptyState {
  grid-column: 1 / -1;
  text-align: center;
  padding: var(--spacing-10) var(--spacing-5);
  border-radius: var(--border-radius-xl);
}

.emptyState h3 {
  font-family: var(--font-family-headings);
  font-size: var(--font-size-xl);
  font-weight: var(--font-weight-bold);
  margin-bottom: var(--spacing-2);
}

.emptyState p {
  font-size: var(--font-size-md);
}

.paginationBar {
  margin-top: var(--spacing-6);
  display: flex;
  justify-content: center;
  align-items: center;
  gap: var(--spacing-3);
}

.paginationInfo {
  font-size: var(--font-size-sm);
  font-weight: var(--font-weight-medium);
  min-width: 100px;
  text-align: center;
}
